<div class="auth-page">
  <div class="auth-card">
    <div class="auth-header">
      <p class="auth-eyebrow">Account access</p>
      <h1 class="auth-title">Resend unlock instructions</h1>
      <p class="auth-subtitle">
        Enter your email address and we'll send a link to unlock your account.
      </p>
    </div>

    <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: "auth-form" }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="auth-field">
        <%= f.label :email %>
        <%= f.email_field :email,
                          autofocus: true,
                          autocomplete: "email",
                          class: "auth-input",
                          placeholder: "name@example.com" %>
      </div>

      <div class="auth-actions">
        <%= f.submit "Resend unlock instructions", class: "auth-submit" %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
</div>
