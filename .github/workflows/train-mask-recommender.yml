name: Train Mask Recommender

on:
  schedule:
    - cron: '0 2 * * 0'
  push:
    tags:
      - 'deploy-ml-*'
  workflow_dispatch:
    inputs:
      target_env:
        description: "Environment to train against (development|staging|production)"
        required: false
        default: "production"

jobs:
  train:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r python/mask_recommender/requirements.txt

      - name: Train neural network model and upload artifacts
        env:
          RAILS_ENV: ${{ github.event.inputs.target_env || 'production' }}
          BREATHESAFE_BASE_URL: ${{ github.event.inputs.target_env == 'staging' && 'https://staging.breathesafe.xyz' || 'https://www.breathesafe.xyz' }}
          BREATHESAFE_SERVICE_EMAIL: ${{ secrets.BREATHESAFE_SERVICE_EMAIL }}
          BREATHESAFE_SERVICE_PASSWORD: ${{ secrets.BREATHESAFE_SERVICE_PASSWORD }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ${{ secrets.AWS_REGION }}
        run: |
          echo "BREATHESAFE_SERVICE_EMAIL set? $([[ -n \"${BREATHESAFE_SERVICE_EMAIL}\" ]] && echo yes || echo no)"
          echo "BREATHESAFE_SERVICE_PASSWORD set? $([[ -n \"${BREATHESAFE_SERVICE_PASSWORD}\" ]] && echo yes || echo no)"
          python python/mask_recommender/train.py --epochs 600 --learning-rate=0.0001 --retrain-with-full
  deploy:
    needs: train
    runs-on: ubuntu-latest
    environment: production
    if: startsWith(github.ref, 'refs/tags/deploy-ml-')
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Ensure Lambda role can read/write model artifacts
        env:
          ROLE_NAME: lambda-ex
          POLICY_NAME: MaskRecommenderArtifactsAccess
        run: |
          set -euo pipefail
          cat > /tmp/lambda-s3-artifacts-policy.json <<'JSON'
          {
            "Version": "2012-10-17",
            "Statement": [
              {
                "Sid": "ListArtifactBuckets",
                "Effect": "Allow",
                "Action": [
                  "s3:ListBucket"
                ],
                "Resource": [
                  "arn:aws:s3:::breathesafe",
                  "arn:aws:s3:::breathesafe-development"
                ]
              },
              {
                "Sid": "ReadWriteArtifactObjects",
                "Effect": "Allow",
                "Action": [
                  "s3:GetObject",
                  "s3:PutObject"
                ],
                "Resource": [
                  "arn:aws:s3:::breathesafe/mask_recommender/models/*",
                  "arn:aws:s3:::breathesafe-development/mask_recommender/models/*"
                ]
              }
            ]
          }
          JSON
          set +e
          POLICY_ERR=$(aws iam put-role-policy \
            --role-name "$ROLE_NAME" \
            --policy-name "$POLICY_NAME" \
            --policy-document file:///tmp/lambda-s3-artifacts-policy.json \
            2>&1)
          POLICY_STATUS=$?
          set -e
          if [ "$POLICY_STATUS" -ne 0 ]; then
            if echo "$POLICY_ERR" | grep -q "AccessDenied"; then
              echo "Warning: unable to update IAM role policy from workflow (AccessDenied). Continuing deploy."
              echo "$POLICY_ERR"
            else
              echo "$POLICY_ERR"
              exit "$POLICY_STATUS"
            fi
          fi

      - name: Login to Amazon ECR
        id: ecr-login
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push Lambda image
        id: build-image
        env:
          REGISTRY: ${{ steps.ecr-login.outputs.registry }}
          REPO: mask-recommender
          IMAGE_TAG: ${{ github.sha }}
        run: |
          set -euo pipefail
          IMAGE_URI="$REGISTRY/$REPO:$IMAGE_TAG"
          LATEST_URI="$REGISTRY/$REPO:latest"

          aws ecr describe-repositories --repository-names "$REPO" >/dev/null 2>&1 || \
            aws ecr create-repository --repository-name "$REPO" >/dev/null

          docker build \
            -f python/mask_recommender/inference/Dockerfile \
            -t "$IMAGE_URI" \
            -t "$LATEST_URI" \
            python/mask_recommender

          docker push "$IMAGE_URI"
          docker push "$LATEST_URI"

          IMAGE_DIGEST=$(aws ecr describe-images \
            --repository-name "$REPO" \
            --image-ids imageTag="$IMAGE_TAG" \
            --query 'imageDetails[0].imageDigest' \
            --output text)

          echo "image_uri_tag=$IMAGE_URI" >> $GITHUB_OUTPUT
          echo "image_uri_latest=$LATEST_URI" >> $GITHUB_OUTPUT
          echo "image_digest=$IMAGE_DIGEST" >> $GITHUB_OUTPUT

      - name: Update Lambda code (staging)
        env:
          FUNCTION_NAME: mask-recommender-staging
          IMAGE_URI: ${{ steps.build-image.outputs.image_uri_tag }}
          IMAGE_TAG: ${{ github.sha }}
          IMAGE_DIGEST: ${{ steps.build-image.outputs.image_digest }}
          ENVIRONMENT: staging
          BREATHESAFE_SERVICE_EMAIL: ${{ secrets.BREATHESAFE_SERVICE_EMAIL }}
          BREATHESAFE_SERVICE_PASSWORD: ${{ secrets.BREATHESAFE_SERVICE_PASSWORD }}
        run: |
          set -euo pipefail
          aws lambda update-function-code \
            --function-name "$FUNCTION_NAME" \
            --image-uri "$IMAGE_URI" \
            >/dev/null
          aws lambda wait function-updated --function-name "$FUNCTION_NAME"
          ENV_JSON=$(aws lambda get-function-configuration --function-name "$FUNCTION_NAME" --query 'Environment.Variables' --output json)
          python - <<'PY' > /tmp/lambda_env.json
          import json
          import os

          env = json.loads(os.environ.get("ENV_JSON") or "{}")
          if env is None:
              env = {}
          env["IMAGE_TAG"] = os.environ.get("IMAGE_TAG")
          env["IMAGE_DIGEST"] = os.environ.get("IMAGE_DIGEST")
          env["ENVIRONMENT"] = os.environ.get("ENVIRONMENT")
          email = os.environ.get("BREATHESAFE_SERVICE_EMAIL")
          password = os.environ.get("BREATHESAFE_SERVICE_PASSWORD")
          if email:
              env["BREATHESAFE_SERVICE_EMAIL"] = email
          if password:
              env["BREATHESAFE_SERVICE_PASSWORD"] = password
          print(json.dumps({"Variables": env}))
          PY
          aws lambda update-function-configuration \
            --function-name "$FUNCTION_NAME" \
            --environment file:///tmp/lambda_env.json \
            >/dev/null
          aws lambda wait function-updated --function-name "$FUNCTION_NAME"

      - name: Update Lambda code (production)
        env:
          FUNCTION_NAME: mask-recommender-production
          IMAGE_URI: ${{ steps.build-image.outputs.image_uri_tag }}
          IMAGE_TAG: ${{ github.sha }}
          IMAGE_DIGEST: ${{ steps.build-image.outputs.image_digest }}
          ENVIRONMENT: production
          BREATHESAFE_SERVICE_EMAIL: ${{ secrets.BREATHESAFE_SERVICE_EMAIL }}
          BREATHESAFE_SERVICE_PASSWORD: ${{ secrets.BREATHESAFE_SERVICE_PASSWORD }}
        run: |
          set -euo pipefail
          aws lambda update-function-code \
            --function-name "$FUNCTION_NAME" \
            --image-uri "$IMAGE_URI" \
            >/dev/null
          aws lambda wait function-updated --function-name "$FUNCTION_NAME"
          ENV_JSON=$(aws lambda get-function-configuration --function-name "$FUNCTION_NAME" --query 'Environment.Variables' --output json)
          python - <<'PY' > /tmp/lambda_env.json
          import json
          import os

          env = json.loads(os.environ.get("ENV_JSON") or "{}")
          if env is None:
              env = {}
          env["IMAGE_TAG"] = os.environ.get("IMAGE_TAG")
          env["IMAGE_DIGEST"] = os.environ.get("IMAGE_DIGEST")
          env["ENVIRONMENT"] = os.environ.get("ENVIRONMENT")
          email = os.environ.get("BREATHESAFE_SERVICE_EMAIL")
          password = os.environ.get("BREATHESAFE_SERVICE_PASSWORD")
          if email:
              env["BREATHESAFE_SERVICE_EMAIL"] = email
          if password:
              env["BREATHESAFE_SERVICE_PASSWORD"] = password
          print(json.dumps({"Variables": env}))
          PY
          aws lambda update-function-configuration \
            --function-name "$FUNCTION_NAME" \
            --environment file:///tmp/lambda_env.json \
            >/dev/null
          aws lambda wait function-updated --function-name "$FUNCTION_NAME"
